import { PwdValidationDirective } from './pwd-validation.directive';

describe('PwdValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdValidationDirective();
    expect(directive).toBeTruthy();
  });
});
